# Dependencies
node_modules/
# zed-pkg: dependency tree and scratch output. .zpkg.lock is COMMITTED —
# `zed install --frozen` requires it even at zero dependencies.
zed_modules/
.zed/

# Build and framework output
dist/
.next/
.turbo/
.vercel/
*.tsbuildinfo
# Generated npm release slice; package.json and .zpkg-smoke.sh remain tracked.
sdk/nodejs/dist/
sdk/nodejs/src/
sdk/nodejs/README.md
sdk/nodejs/LICENSE
sdk/nodejs/.cli-flags.toml
sdk/java/target/
sdk/java/.build/
sdk/dart/.dart_tool/
sdk/dart/pubspec.lock
sdk/erlang/_build/
sdk/erlang/.build/
sdk/elixir/_build/
sdk/elixir/deps/

# Tests and tool caches
coverage/
.nyc_output/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.out
# Generated browser-conformance entry (must live in-package for self-reference)
.conformance-tmp/
.wrangler/
.eslintcache
.cache/

# Package artifacts and logs
*.tgz
*.tar
*.tar.gz
*.gem
*.whl
*.egg-info/
sdk/python/build/
sdk/python/dist/
sdk/rust/target/
/target/
sdk/rust-otel/target/
sdk/rust-otel/Cargo.lock
sdk/rust-context/target/
sdk/rust-context/Cargo.lock
sdk/wasm/target/
sdk/gleam/build/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local environment files (examples remain trackable)
.env
.env.*
!.env.example
!.env.*.example

# Editors and operating systems
.DS_Store
Thumbs.db
.idea/
.vscode/
tmp
temp
tmp/worktrees
temp/worktrees

# --- environment / secrets (sops + age) -------------------------------------
# Only env/enc/*.env.enc is tracked. Every plaintext form stays out of git;
# `just env-check` enforces it. See env/README.md.
*.env
**/*.env
env/dec/

# age private keys — never commit these.
*.agekey
*.age-key
keys.txt
sops-private*.txt
