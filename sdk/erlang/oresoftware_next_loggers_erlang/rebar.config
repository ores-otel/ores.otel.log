%% Rebar3 requires the basename of a git_subdir dependency path to match the
%% OTP application name. Keep the implementation canonical in ../src and point
%% this dependency-layout shim at it rather than duplicating logger sources.
{erl_opts, [debug_info, warnings_as_errors]}.
{src_dirs, ["../src"]}.
