%% Rebar3 requires the basename of a git_subdir dependency path to match the
%% OTP application name. This exported subproject is kept byte-identical with
%% the canonical sdk/erlang/src implementation by CI.
{erl_opts, [debug_info, warnings_as_errors]}.
